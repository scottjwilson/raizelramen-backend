{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "name": "Order",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "user": {
      "via": "orders",
      "plugin": "users-permissions",
      "model": "user"
    },
    "uuid": {
      "default": "",
      "type": "uuid"
    },
    "charge": {
      "type": "string"
    },
    "line_items": {
      "type": "json"
    },
    "products": {
      "collection": "product"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "pending",
        "inprogress",
        "completed"
      ],
      "default": "pending"
    },
    "order_status": {
      "via": "order",
      "model": "status"
    }
  }
}
